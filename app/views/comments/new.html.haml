.container
  %ul
    - @comment.errors.full_messages.each do |message|
      %li = %message

  = form_with(model: @comment, url: board_task_comments_path(@comment.task), method: 'post', local: true) do |f|
    %div
      = f.label :content, 'コメントの内容'
    %div
      = f.text_area :content
    = f.submit 'コメントを保存', class: 'btn-primary'
    
    
    